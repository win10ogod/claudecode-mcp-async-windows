{
  "mcpServers": {
    "claude-code-mcp": {
      "command": "uvx",
      "args": [
        "claudecode-mcp-async"
      ],
      "description": "Claude Code MCP Server - Execute Claude Code tasks asynchronously"
    }
  }
}
