{
  "mcpServers": {
    "claude-code-mcp": {
      "command": "uvx",
      "args": [
        "claudecode-mcp-async-windows"
      ],
      "description": "Claude Code MCP Server - Execute Claude Code tasks asynchronously (Windows, Linux, macOS)"
    }
  }
}
